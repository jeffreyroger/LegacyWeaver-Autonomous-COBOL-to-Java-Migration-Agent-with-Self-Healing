"""LegacyWeaver run service — the transport/lifecycle layer between the
agent and the frontend. Contains no domain logic: it starts runs, tracks
their lifecycle, and forwards events. See docs/specs/BACKEND_PLAN.md §1.2.
"""
